{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Open 40 Love in Chrome (run npm start first)",
      "type": "chrome",
      "request": "launch",
      "url": "http://localhost:5173",
      "webRoot": "${workspaceFolder}/src"
    }
  ]
}
